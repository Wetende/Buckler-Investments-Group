"""
MVP Test Configuration

Simple configuration for MVP endpoint testing with real database.
No mocks - only real database integration testing.
"""
import pytest
import asyncio


@pytest.fixture(scope="session")
def event_loop():
    """Create an instance of the default event loop for the test session."""
    loop = asyncio.get_event_loop_policy().new_event_loop()
    yield loop
    loop.close()

